- args:
    sql: CREATE TABLE "user"("name" text NOT NULL, "email" text NOT NULL, "address"
      text NOT NULL, "lat" text NOT NULL, "lon" text NOT NULL, "radius" text NOT NULL,
      PRIMARY KEY ("email") )
  type: run_sql
- args:
    name: user
  type: add_existing_table_or_view
